package ListofNotes;
import Notes.Note;

//class for A Hundred note
public class Hundred extends Note {
    public Hundred(String note, int count) {
        super(note,count);// Set value of note and set value of count
    }
}
