package Notes;

//class for Hundred note
public class Hundred extends Note {
    public Hundred(String note, int count) {
        this.setNote(note);  // Set note value
        this.setCount(count); // Set note count
    }
}
