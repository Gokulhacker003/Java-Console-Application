package Notes;

//class for Two Hundred note
public class Two_Hundred extends Note {
    public Two_Hundred(String note, int count) {
        this.setNote(note);  // Set note value
        this.setCount(count); // Set note count
    }
}

